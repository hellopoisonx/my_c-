package main

import (
	"fmt"
	"strconv"
)
func Max(a uint, )
func reverse_str(a string) []byte {
	var ans []byte
	for i := 0; i < len(a); i ++ {
		for j := len(a) - 1; j > i ; j -- {
			ans[i], ans[j] = a[j], a[i]
		}
	}
	return 
}
func high_plus(a string, b string) string {
	_a := reverse_str(a)
	_b := reverse_str(b)
	var ans []byte
	var next int = 0
	for i := 0; i < len()
}
func main() {

}